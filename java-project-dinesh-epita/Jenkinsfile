pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/dinesh11990/Jenkins_DevOps'
            }
        }

        stage('Build') {
            steps {
                sh 'mvn clean compile'
            }
        }

        stage('Run') {
            steps {
                sh 'mvn exec:java -Dexec.mainClass="com.example.App"'
            }
        }
    }
}
